# 🚀 Инструкция по релизам ML Delivery Analytics

## Автоматическая сборка и публикация

Проект настроен на автоматическую сборку `.exe` файла через GitHub Actions.

### 📦 Как создать новый релиз

#### Вариант 1: Через тег (рекомендуется)

1. **Создайте тег версии:**
   ```bash
   git tag v2.1.0
   git push origin v2.1.0
   ```

2. **GitHub Actions автоматически:**
   - Соберёт `.exe` файл
   - Создаст Release с описанием
   - Загрузит файл в релиз

3. **Пользователи смогут скачать** обновление из раздела [Releases](https://github.com/ваш-репозиторий/releases)

#### Вариант 2: Ручной запуск

1. Перейдите в **Actions** → **Build Windows EXE**
2. Нажмите **Run workflow**
3. Введите версию (например: `v2.1.0`)
4. Нажмите **Run workflow**

### 📥 Как пользователям скачать обновление

1. Перейдите на страницу [Releases](https://github.com/ваш-репозиторий/releases)
2. Найдите последний релиз
3. Скачайте `ML_Delivery_Analytics.exe`
4. Запустите файл (не требует установки)

### 🔄 Обновление для пользователей

При запуске приложение автоматически:
- Проверяет наличие обновлений (можно добавить в будущем)
- Сохраняет настройки и кэш данных

**Ручное обновление:**
1. Скачайте новый `.exe` файл
2. Замените старый файл
3. Запустите новый файл

### 📋 Версионирование

Используем [Semantic Versioning](https://semver.org/):
- **MAJOR** (2.x.x) - несовместимые изменения API
- **MINOR** (x.1.x) - новая функциональность (обратно совместимо)
- **PATCH** (x.x.1) - исправления багов

Примеры:
- `v2.0.0` - первая версия 2.0
- `v2.1.0` - добавлена карта поставок
- `v2.1.1` - исправлена проблема с окнами расписания

### 🛠️ Локальная сборка (для разработчиков)

```bash
cd delivery_analytics
pip install -r ../requirements.txt
pip install pyinstaller
python -m PyInstaller ML_Delivery_Analytics.spec --clean --noconfirm
```

Готовый файл будет в `delivery_analytics/dist/ML_Delivery_Analytics.exe`

### 📝 Что включено в релиз

- ✅ Автоматическая сборка `.exe` через GitHub Actions
- ✅ Автоматическое создание Release при пуше тега
- ✅ Описание изменений из коммитов
- ✅ Хранение артефактов 90 дней
- ✅ Поддержка ручного запуска сборки

### ⚙️ Настройка workflow

Файл `.github/workflows/build-windows-exe.yml` содержит:
- Автоматическую установку зависимостей
- Сборку через PyInstaller
- Создание Release с описанием
- Загрузку артефактов

### 🔍 Проверка сборки

После создания тега:
1. Перейдите в **Actions** → выберите запущенный workflow
2. Дождитесь завершения (обычно 5-10 минут)
3. Проверьте, что Release создан
4. Скачайте и протестируйте `.exe` файл

### ❓ Проблемы

**Сборка не запускается:**
- Проверьте, что тег начинается с `v` (например: `v2.1.0`)
- Убедитесь, что тег запушен: `git push origin v2.1.0`

**Ошибка "Resource not accessible by integration" (403):**
Это означает, что GitHub Actions не имеет прав на создание релизов.

**Решение:**
1. Перейдите в **Settings** → **Actions** → **General**
2. В разделе **Workflow permissions** выберите:
   - ✅ **Read and write permissions** (или **Read repository contents and packages permissions**)
   - ✅ Включите **Allow GitHub Actions to create and approve pull requests**
3. Сохраните изменения
4. Перезапустите workflow

**Альтернативное решение (если настройки не помогают):**
1. Создайте Personal Access Token (PAT) с правами `repo`
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Создайте новый secret: `RELEASE_TOKEN` со значением вашего PAT
4. В workflow замените `GITHUB_TOKEN` на `RELEASE_TOKEN` в секции создания релиза

**Ошибка 404 при открытии релиза:**
Релиз не доступен для других пользователей.

**Причины и решения:**

1. **Репозиторий приватный:**
   - Перейдите в **Settings** → **General** → **Danger Zone** → **Change visibility**
   - Сделайте репозиторий **Public** (или дайте доступ нужным пользователям)

2. **Релиз создан как Draft:**
   - Перейдите в **Releases** → найдите ваш релиз
   - Если он помечен как "Draft", нажмите **Edit** → снимите галочку **This is a pre-release** → **Update release**
   - Или удалите и пересоздайте релиз через workflow

3. **Тег не был запушен:**
   - Убедитесь, что тег запушен: `git push origin v2.1.2`
   - Проверьте, что тег виден в **Releases** или **Tags**

4. **Релиз создан вручную с ошибками:**
   - Удалите релиз вручную
   - Создайте новый через workflow (через тег или ручной запуск)

**Проверка:**
- Откройте ссылку в режиме инкогнито (чтобы проверить без авторизации)
- Убедитесь, что репозиторий публичный или пользователь имеет доступ

**Ошибка при сборке:**
- Проверьте логи в Actions
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте, что `ML_Delivery_Analytics.spec` актуален

**EXE не запускается:**
- Проверьте, что все зависимости включены в `spec` файл
- Убедитесь, что используется правильная версия Python (3.12)
