# 🤖 ML-Аналитика доставок v2.0

## 📋 Описание

**ML-Аналитика доставок** — программа для анализа и предсказания времени привоза товаров с использованием машинного обучения. Система автоматически анализирует исторические данные, выявляет тренды и генерирует рекомендации по корректировке графика поставок.

### 🎯 Основные возможности

- **📊 Анализ исторических данных** — порционная загрузка данных за любой период
- **🤖 ML-предсказание** — использование Gradient Boosting для предсказания отклонений
- **📈 Визуализация** — интерактивные графики и диаграммы
- **💡 Рекомендации** — автоматическая генерация рекомендаций по корректировке расписания
- **🔍 Детекция трендов** — обнаружение паттернов и аномалий
- **📥 Экспорт** — выгрузка результатов в Excel

---

## 🚀 Установка

### Требования

- Python 3.8+
- Windows 10/11 (или Linux/Mac с GUI)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

Или вручную:

```bash
pip install pandas>=2.0.0
pip install openpyxl>=3.1.0
pip install tkcalendar>=1.6.0
pip install requests>=2.31.0
pip install scikit-learn>=1.3.0
pip install numpy>=1.24.0
pip install matplotlib>=3.7.0
```

---

## 📖 Использование

### Запуск программы

```bash
cd delivery_analytics
python ML_Анализ_Доставки.py
```

### Первый запуск

1. **Загрузка данных:**

   - Выберите период в календарях
   - Нажмите **"📥 Период"** для загрузки за выбранный период
   - Или **"📚 История"** для загрузки всех данных с 2023 года (займет 5-15 минут)
2. **Обучение модели:**

   - После загрузки данных модель обучается автоматически
   - Статус отображается в нижней панели
3. **Просмотр результатов:**

   - Вкладка **"📊 Статистика поставщиков"** — общая статистика
   - Вкладка **"🤖 ML-Рекомендации"** — рекомендации по корректировке

---

## 🎨 Интерфейс

### Главное окно

```
┌─────────────────────────────────────────────────────────┐
│  🤖 ML-Аналитика доставок                               │
│  Машинное обучение для оптимизации графика поставок    │
├─────────────────────────────────────────────────────────┤
│  📅 Период: [01.11.2025] — [27.11.2025]                │
│  📥 Загрузка: [Период] [История] [Кэш]                  │
│  🔍 Анализ: [Переобучить] [Графики] [Экспорт]          │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────┬──────────────────────────────┐   │
│  │ 📊 Статистика   │ 🤖 ML-Рекомендации          │   │
│  │                 │                              │   │
│  │ [Таблица]       │ [Таблица рекомендаций]      │   │
│  └─────────────────┴──────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### Вкладки

#### 1. 📊 Статистика поставщиков

Таблица с метриками по каждому поставщику:

| Столбец               | Описание                                              |
| ---------------------------- | ------------------------------------------------------------- |
| **Поставщик** | Название поставщика                         |
| **Склад**         | Название склада                                 |
| **Заказов**     | Количество уникальных заказов      |
| **Ср. откл.**    | Среднее отклонение от плана (мин)  |
| **Медиана**     | Медианное отклонение (мин)              |
| **Ст. откл.**    | Стандартное отклонение                   |
| **% вовремя**   | Процент заказов в пределах ±30 мин |

**Цветовая индикация:**

- 🟢 Зелёный — ≥80% вовремя
- 🟡 Жёлтый — 60-80% вовремя
- 🔴 Красный — <60% вовремя

**Действия:**

- **Клик на заголовок** — сортировка по столбцу (▲/▼)
- **Двойной клик** — открыть детальный анализ поставщика

#### 2. 🤖 ML-Рекомендации

Таблица с рекомендациями по корректировке расписания:

| Столбец                   | Описание                                         |
| -------------------------------- | -------------------------------------------------------- |
| **Поставщик**     | Название поставщика                    |
| **Склад**             | Название склада                            |
| **День**               | День недели                                    |
| **Час**                 | Час заказа                                      |
| **Сдвиг**             | Рекомендуемый сдвиг (мин)           |
| **Уверенность** | Уверенность модели (0-100%)             |
| **Тренд**             | Обнаруженный тренд                      |
| **Применить с**  | Рекомендуемая дата применения |

**Цветовая индикация:**

- 🟢 Зелёный фон — уверенность ≥80%
- 🟡 Жёлтый фон — уверенность 60-80%
- 🟠 Оранжевый фон — уверенность <60%

**Действия:**

- **Двойной клик** — открыть подробности рекомендации

---

## 🔧 Функции программы

### Загрузка данных

#### `fetch_data()`

Загружает данные за выбранный период.

**Параметры:** Использует даты из календарей `cal_start` и `cal_end`

**Процесс:**

1. Разбивает период на части по 14 дней
2. Загружает каждую часть отдельно
3. Объединяет результаты
4. Автоматически запускает обучение модели

#### `fetch_historical_data()`

Загружает все данные с января 2023 года по текущую дату.

**Особенности:**

- Порционная загрузка (по 14 дней)
- Сохранение в кэш (`ml_data_cache.pkl`)
- Длительность: 5-15 минут

#### `load_cached_data()`

Быстрая загрузка из сохранённого кэша.

**Требования:** Файл `ml_data_cache.pkl` должен существовать

### ML-обучение

#### `train_model_async()`

Асинхронное обучение ML-модели.

**Алгоритм:**

- **Gradient Boosting Regressor** (100 деревьев, глубина 5)
- Отдельная модель для каждой пары (поставщик, склад)
- Минимум 10 записей для обучения

**Признаки модели:**

- День недели (циклическое кодирование)
- Час заказа
- Неделя/месяц года
- Скользящие средние (3, 7, 14 дней)
- Тренд за последние дни

**Результат:**

- Обученные модели в `ml_predictor.models`
- Список рекомендаций в `recommendations`

### Анализ и визуализация

#### `show_supplier_details(supplier, warehouse)`

Открывает окно с детальным анализом поставщика.

**Вкладки:**

1. **📈 Графики** — 4 интерактивных графика:

   - Распределение отклонений
   - Отклонение по дням недели
   - Отклонение по часам заказа
   - Тренд по датам (7-дневное скользящее среднее)
2. **📅 По дням** — статистика по каждому дню недели
3. **⏰ По часам** — статистика по часам заказа (6:00-22:00)
4. **📋 Заказы** — список последних 500 заказов

#### `show_overall_charts()`

Общие графики по всем данным:

- Топ-10 поставщиков по опозданиям
- Распределение заказов по дням недели
- Общее распределение отклонений
- Динамика по месяцам

#### `show_recommendation_details(rec)`

Детальная информация о рекомендации:

- Параметры (поставщик, склад, день, час)
- Текущее и рекомендуемое время
- Причина рекомендации
- Кнопки: "Анализ поставщика", "Экспорт в Excel"

### Экспорт

#### `export_all_recommendations()`

Экспорт всех рекомендаций в Excel.

**Формат:**

- Файл: `ML_Рекомендации_YYYYMMDD.xlsx`
- Форматированные заголовки
- Автоматическая ширина столбцов

#### `export_single_rec(rec)`

Экспорт одной рекомендации в Excel.

---

## 📊 ML-Модель

### Архитектура

```
DeliveryMLPredictor
├── prepare_features()      # Подготовка признаков
├── add_rolling_features() # Скользящие средние
├── fit()                   # Обучение модели
├── predict()               # Предсказание
├── detect_trend()          # Детекция тренда
└── generate_recommendations() # Генерация рекомендаций
```

### Типы трендов

| Тип       | Описание                                  |
| ------------ | ------------------------------------------------- |
| `stable`   | Стабильный график                 |
| `delay`    | Увеличивающиеся опоздания |
| `early`    | Ранние привозы                       |
| `shift`    | Резкий сдвиг времени            |
| `seasonal` | Сезонный паттерн                   |

### Генерация рекомендаций

Рекомендация создаётся если:

- Минимум 5 записей в группе (поставщик-склад-день-час)
- Сдвиг между первой и второй половиной периода ≥15 минут
- Систематическое отклонение от графика

**Формула уверенности:**

```
confidence = max(0.5, min(0.95, 1 - std_deviation / 60))
```

---

## 🗂️ Структура файлов

```
delivery_analytics/
├── ML_Анализ_Доставки.py    # Основная программа
├── ml_predictor.py           # ML-модуль
├── ml_data_cache.pkl         # Кэш данных (создаётся автоматически)
├── README_ML.md              # Эта документация
└── requirements.txt          # Зависимости
```

---

## ⚙️ Настройки

### Изменение размера порции загрузки

В функции `fetch_data_chunked()`:

```python
def fetch_data_chunked(start_date, end_date, chunk_days=14):
    # Измените chunk_days (по умолчанию 14 дней)
```

### Изменение параметров ML-модели

В файле `ml_predictor.py`, класс `DeliveryMLPredictor.fit()`:

```python
model = GradientBoostingRegressor(
    n_estimators=100,    # Количество деревьев
    max_depth=5,         # Глубина дерева
    learning_rate=0.1,   # Скорость обучения
    random_state=42
)
```

### Изменение порогов рекомендаций

В функции `train_model_async()`:

```python
recommendations = ml_predictor.generate_recommendations(
    df_current, 
    min_samples=5,    # Минимум записей
    min_shift=15      # Минимум сдвига (мин)
)
```

---

## 🐛 Решение проблем

### Ошибка: "Нет данных для обучения"

**Причина:** Недостаточно записей в данных.

**Решение:**

- Загрузите данные за больший период
- Используйте кнопку "📚 История" для загрузки всех данных

### Ошибка: "Input y contains NaN"

**Причина:** В данных есть пустые значения в столбце отклонений.

**Решение:** Уже исправлено в коде — пустые значения автоматически удаляются.

### Медленная загрузка

**Причина:** Большой период или медленный интернет.

**Решение:**

- Используйте кэш (кнопка "💾 Кэш")
- Уменьшите период загрузки
- Проверьте скорость интернета

### Графики не отображаются

**Причина:** Не установлен matplotlib или проблемы с шрифтами.

**Решение:**

```bash
pip install matplotlib --upgrade
```

---

## 📝 Примеры использования

### Пример 1: Анализ конкретного поставщика

1. Загрузите данные
2. Перейдите на вкладку "📊 Статистика"
3. Найдите поставщика в таблице
4. Двойной клик по строке
5. Изучите графики и статистику

### Пример 2: Получение рекомендаций

1. Загрузите исторические данные (📚 История)
2. Дождитесь обучения модели
3. Перейдите на вкладку "🤖 ML-Рекомендации"
4. Отсортируйте по уверенности (клик на заголовок)
5. Двойной клик по рекомендации для деталей

### Пример 3: Экспорт результатов

1. После получения рекомендаций
2. Нажмите "📥 Экспорт"
3. Выберите место сохранения
4. Откройте Excel-файл

---

## 🔐 Безопасность

- **Данные:** Хранятся локально в файле `ml_data_cache.pkl`
- **Сеть:** Используется HTTPS для загрузки данных
- **Авторизация:** Требуется авторизация в CRM через браузер

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте версии библиотек: `pip list`
2. Проверьте логи в консоли
3. Убедитесь, что данные загружены корректно
4. Попробуйте переобучить модель (кнопка "🔄 Переобучить")

---

## 📄 Лицензия

Внутренний инструмент для анализа доставок.

**Версия:** 2.0
**Дата:** 2025 ноябрь 27
**Автор:** AI Assistant + BezdomnyiBox feat Aleksey
