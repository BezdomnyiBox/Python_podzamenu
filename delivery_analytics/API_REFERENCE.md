# 📚 API Reference — ML-Аналитика доставок

## Классы

### `SortableTreeview`

Расширенный `ttk.Treeview` с поддержкой сортировки по столбцам.

**Методы:**
- `sort_by(col)` — сортировка по указанному столбцу

**Использование:**
```python
tree = SortableTreeview(parent, columns=('Col1', 'Col2'))
tree.heading('Col1', command=lambda: tree.sort_by('Col1'))
```

---

## Функции загрузки данных

### `fetch_data()`
Загружает данные за выбранный период из календарей.

**Возвращает:** `None` (обновляет глобальные переменные)

**Побочные эффекты:**
- Обновляет `df_current`
- Запускает `train_model_async()`

---

### `fetch_data_chunked(start_date, end_date, chunk_days=14)`

Порционная загрузка данных с сервера.

**Параметры:**
- `start_date` (date) — начальная дата
- `end_date` (date) — конечная дата
- `chunk_days` (int) — размер порции в днях (по умолчанию 14)

**Возвращает:**
- `pd.DataFrame` — объединённые данные или `None` при ошибке

**Пример:**
```python
from datetime import date
df = fetch_data_chunked(date(2023, 1, 1), date(2023, 12, 31))
```

---

### `fetch_historical_data()`

Загружает все данные с 2023 года.

**Возвращает:** `None` (обновляет глобальные переменные и сохраняет в кэш)

---

### `load_cached_data()`

Загружает данные из файла `ml_data_cache.pkl`.

**Возвращает:** `None`

**Исключения:**
- `FileNotFoundError` — если кэш не существует

---

## Функции ML

### `train_model_async()`

Асинхронное обучение ML-модели.

**Требования:**
- `df_current` должен быть загружен

**Побочные эффекты:**
- Обновляет `ml_predictor`
- Обновляет `recommendations`
- Устанавливает `is_model_trained = True`

---

### `retrain_model()`

Переобучение модели на текущих данных.

**Проверки:**
- Проверяет наличие `df_current`

---

## Функции отображения

### `update_stats_display()`

Обновляет таблицу статистики поставщиков.

**Требования:**
- `df_current` должен быть загружен

**Обновляет:**
- `tree_stats` — таблица статистики
- `lbl_stats_count` — счётчик поставщиков

---

### `update_recommendations_display()`

Обновляет таблицу рекомендаций.

**Требования:**
- `recommendations` должен быть заполнен

**Обновляет:**
- `tree_rec` — таблица рекомендаций
- `lbl_rec_count` — счётчик рекомендаций

---

### `update_status(text, status_type="info")`

Обновляет статус-бар.

**Параметры:**
- `text` (str) — текст статуса
- `status_type` (str) — тип: "info", "success", "warning", "error"

**Цвета:**
- `info` → синий
- `success` → зелёный
- `warning` → оранжевый
- `error` → красный

---

## Функции детализации

### `show_supplier_details(supplier, warehouse)`

Открывает окно с детальным анализом поставщика.

**Параметры:**
- `supplier` (str) — название поставщика
- `warehouse` (str) — название склада

**Создаёт:**
- Окно `tk.Toplevel` с 4 вкладками:
  1. Графики (4 интерактивных графика)
  2. По дням недели (таблица)
  3. По часам (таблица)
  4. Заказы (таблица последних 500)

---

### `create_supplier_charts(parent, df, supplier)`

Создаёт 4 графика для поставщика.

**Параметры:**
- `parent` (tk.Widget) — родительский виджет
- `df` (pd.DataFrame) — данные поставщика
- `supplier` (str) — название (для заголовка)

**Графики:**
1. Гистограмма распределения отклонений
2. Столбчатая диаграмма по дням недели
3. Линейный график по часам
4. Тренд по датам (со скользящим средним)

---

### `show_recommendation_details(rec)`

Открывает окно с деталями рекомендации.

**Параметры:**
- `rec` (ScheduleRecommendation) — объект рекомендации

**Отображает:**
- Параметры (поставщик, склад, день, час)
- Текущее и рекомендуемое время
- Причину рекомендации
- Кнопки действий

---

### `show_overall_charts()`

Открывает окно с общими графиками по всем данным.

**Требования:**
- `df_current` должен быть загружен

**Графики:**
1. Топ-10 поставщиков по опозданиям
2. Распределение заказов по дням недели
3. Общее распределение отклонений
4. Динамика по месяцам

---

## Функции экспорта

### `export_all_recommendations()`

Экспортирует все рекомендации в Excel.

**Формат файла:**
- `ML_Рекомендации_YYYYMMDD.xlsx`

**Столбцы:**
- Поставщик, Склад, День, Час заказа, Сдвиг, Уверенность, Тренд, Причина, Применить с

---

### `export_single_rec(rec)`

Экспортирует одну рекомендацию в Excel.

**Параметры:**
- `rec` (ScheduleRecommendation) — рекомендация

**Формат:**
- Таблица "Параметр" | "Значение"

---

## Обработчики событий

### `on_stats_double_click(event)`

Обработчик двойного клика по таблице статистики.

**Действие:**
- Открывает `show_supplier_details()` для выбранного поставщика

---

### `on_rec_double_click(event)`

Обработчик двойного клика по таблице рекомендаций.

**Действие:**
- Открывает `show_recommendation_details()` для выбранной рекомендации

---

## Глобальные переменные

| Переменная | Тип | Описание |
|------------|-----|----------|
| `df_original` | `pd.DataFrame` | Исходные данные |
| `df_current` | `pd.DataFrame` | Текущие данные (после фильтрации) |
| `ml_predictor` | `DeliveryMLPredictor` | Обученная ML-модель |
| `recommendations` | `List[ScheduleRecommendation]` | Список рекомендаций |
| `is_model_trained` | `bool` | Флаг обучения модели |

---

## Константы

### `COLORS`

Цветовая схема интерфейса:

```python
COLORS = {
    'bg': '#f0f2f5',           # Фон
    'header': '#1a237e',        # Заголовок
    'primary': '#3f51b5',       # Основной цвет
    'success': '#4caf50',       # Успех
    'warning': '#ff9800',       # Предупреждение
    'danger': '#f44336',        # Ошибка
    'info': '#2196f3',          # Информация
    'text': '#212121',          # Текст
    'text_light': '#757575',    # Светлый текст
    'card': '#ffffff'           # Карточка
}
```

### `DAYS_RU`

Список дней недели на русском:
```python
["Понедельник", "Вторник", "Среда", "Четверг", "Пятница", "Суббота", "Воскресенье"]
```

### `DAYS_SHORT`

Сокращённые названия дней:
```python
["Пн", "Вт", "Ср", "Чт", "Пт", "Сб", "Вс"]
```

---

## Примеры использования API

### Пример 1: Программная загрузка данных

```python
from datetime import date, timedelta

# Загрузить данные за последний месяц
end_date = date.today()
start_date = end_date - timedelta(days=30)

df = fetch_data_chunked(start_date, end_date)
if df is not None:
    print(f"Загружено {len(df)} записей")
```

### Пример 2: Получение рекомендаций для конкретного поставщика

```python
# После обучения модели
if is_model_trained:
    supplier_recs = [r for r in recommendations if r.supplier == "Авто МС"]
    for rec in supplier_recs:
        print(f"{rec.weekday}: сдвиг {rec.shift_minutes:+d} мин")
```

### Пример 3: Анализ конкретного поставщика

```python
# Открыть детальный анализ
show_supplier_details("Авто МС", "Москва")
```

---

## Интеграция с ml_predictor.py

### Использование ML-модуля напрямую

```python
from ml_predictor import DeliveryMLPredictor

# Создать предиктор
predictor = DeliveryMLPredictor()

# Обучить на данных
predictor.fit(df_current)

# Получить рекомендации
recommendations = predictor.generate_recommendations(
    df_current,
    min_samples=5,
    min_shift=15
)

# Предсказать для конкретных параметров
prediction = predictor.predict(
    df_current,
    supplier="Авто МС",
    warehouse="Москва",
    weekday=1,  # Вторник
    hour=10      # 10:00
)
```

---

## Расширение функциональности

### Добавление нового графика

```python
def create_custom_chart(parent, df):
    fig = Figure(figsize=(8, 6), dpi=100)
    ax = fig.add_subplot(111)
    
    # Ваш код графика
    ax.plot(...)
    
    canvas = FigureCanvasTkAgg(fig, parent)
    canvas.draw()
    canvas.get_tk_widget().pack()
```

### Добавление нового фильтра

```python
def apply_custom_filter():
    global df_current
    # Применить фильтр
    df_current = df_current[df_current['Поставщик'] == "Авто МС"]
    update_stats_display()
```

---

**Версия документации:** 1.0  
**Дата:** 2025

